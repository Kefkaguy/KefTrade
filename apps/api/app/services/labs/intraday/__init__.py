"""Intraday Research Lab.

Step 1 scope only: session-aware feature computation (`session.py`,
`features.py`). Strategy blocks, campaign generation, and validation-rule
wiring into the elite gate are explicitly out of scope until a later step.
"""
